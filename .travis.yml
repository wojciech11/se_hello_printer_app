language: python
services:
  - docker
python:
  - "2.7"
inslall:
  - make deps
script:
  - make lint
  - make test_xunit
  - make test_cov
after_success:
  - sudo make docker_build
  - sudo make docker_push
deploy:
    provider: heroku
    app: still-ridge-87834
    api_key: ${HEROKU_API_KEY}
